from setuptools import setup, find_packages

setup(
    name="horizon_tab_theme",
    version="0.2.0",
    description="Horizon Tab Theme (formerly Horizon Command) — the flagship dense Desk theme for Horizon SaaS tenants, top tab bar instead of a sidebar",
    author="Horizon Smart Systems",
    author_email="support@horizonerp.cloud",
    packages=find_packages(),
    zip_safe=False,
    include_package_data=True,
    install_requires=[],
)
